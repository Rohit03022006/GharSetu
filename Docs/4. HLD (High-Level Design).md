# High-Level Design (HLD) Document
## Real Estate Management & Property Discovery Platform

**Version:** 1.0
**Status:** Draft
**Prepared By:** Rohit
**Last Updated:** July 2026


---

## 1. Purpose

This HLD gives a visual, system-level view of the platform: how the 7 microservices fit together, how data flows through the system, what each service owns, and where external integrations plug in. Where the SRS specifies *requirements* (FR/NFR/API contracts), this document specifies *structure* — the shape of the system a developer or reviewer should hold in their head before diving into any one service.

---

## 2. Overall System Architecture

```mermaid
graph LR
    User((User))
    User --> React["React SPA"]
    React --> Gateway["Nginx Gateway"]
    subgraph Microservices
        Identity["Identity"]
        Listing["Listing"]
        Finance["Finance"]
        Preference["Preference"]
        Discovery["Discovery"]
        Engagement["Engagement"]
        Analytics["Analytics"]
    end
    Gateway --> Identity
    Gateway --> Listing
    Gateway --> Finance
    Gateway --> Preference
    Gateway --> Discovery
    Gateway --> Engagement
    Gateway --> Analytics
    Identity -->|"Auth"| Google["Google OAuth2"]
    Listing -->|"Maps"| OSM["OpenStreetMap"]
    Engagement -->|"Email"| SMTP["SMTP"]
    Engagement -->|"SMS"| SMS["SMS Gateway"]

    subgraph Infrastructure
        Redis[(Redis Cache / PubSub)]
        MinIO[(MinIO Storage)]
    end

    Listing --- Redis
    Discovery --- Redis
    Preference --- Redis
    Engagement --- Redis
    Analytics --- Redis

    Identity --> MinIO
    Listing --> MinIO

    subgraph Databases
        ID[(Identity DB)]
        LD[(Listing DB)]
        FD[(Finance DB)]
        PD[(Preference DB)]
        DD[(Discovery DB)]
        ED[(Engagement DB)]
        AD[(Analytics DB)]

    end

    Identity --> ID
    Listing --> LD
    Finance --> FD
    Preference --> PD
    Discovery --> DD
    Engagement --> ED
    Analytics --> AD

    Listing --> Engagement
```

**Reading this diagram:** Each service has its own `db` node next to it — the **database-per-service** pattern (no service reaches into another's DB directly). Redis sits centrally, connected to every service that needs it as either a read cache or a Pub/Sub event bus. The Listing↔Engagement link at the bottom is the review-eligibility check called out in the SRS as a deliberate, explicit cross-service coupling — every other cross-service interaction flows through the gateway or Redis.

---

## 3. Modules & Services

```mermaid
graph LR
    subgraph "1. Identity Service"
        A1[Auth - Register/Login/JWT]
        A2[OAuth2 - Google]
        A3[User & Role Management]
        A4[Verification Workflow]
    end

    subgraph "2. Listing Service"
        B1[Property CRUD + Approval]
        B2[Search & Filters]
        B3[Reviews & Ratings]
        B4[Property Sharing]
        B5[Content Moderation]
    end

    subgraph "3. Finance Service"
        C1[EMI Calculator]
        C2[Stamp Duty / Registration]
        C3[GST Calculator]
        C4[Maintenance Estimator]
    end

    subgraph "4. Preference Service"
        D1[Wishlist]
        D2[Compare]
    end

    subgraph "5. Discovery-History Service"
        E1[Recently Viewed - last 20]
        E2[Similar Properties]
    end

    subgraph "6. Engagement Service"
        F1[Lead Pipeline]
        F2[Booking & Availability]
        F3[Notifications]
    end

    subgraph "7. Analytics Service"
        G1[Property/Sales Analytics]
        G2[Admin Platform Dashboard]
    end
```

### 3.1 Service Responsibility Summary

| Service | Primary Responsibility | Key Data Owned |
|---|---|---|
| **Identity** | Who the user is, what they're allowed to do | Users, Roles, Verification status |
| **Listing** | The source of truth for every property, its approval state, and its reputation (reviews) | Properties, Images, Reviews |
| **Finance** | Stateless money-math for buyers | Finance_Rates (config) |
| **Preference** | What a buyer likes / wants to compare | Wishlist, Compare_History |
| **Discovery-History** | What a buyer has seen and what to show them next | Recently_Viewed, Search_History |
| **Engagement** | Turning interest into a closed deal | Leads, Bookings, Availability, Notifications |
| **Analytics** | Making sense of what happened across the platform | Aggregated metrics (event-sourced) |

---

## 4. Database Overview

### 4.1 Database-per-Service Map

```mermaid
graph TB
    subgraph identity_db
        U[Users]
        R[Roles/Permissions]
        V[Verification_Documents]
    end

    subgraph listing_db
        P[Properties]
        PI[Property_Images]
        RV[Reviews]
        AM[Amenities]
        CI[Cities/Locations]
    end

    subgraph finance_db
        FR[Finance_Rates]
    end

    subgraph preference_db
        WL[Wishlist]
        CH[Compare_History]
    end

    subgraph discovery_db
        RVW[Recently_Viewed]
        SH[Search_History]
    end

    subgraph engagement_db
        LD[Leads]
        LSH[Lead_Stage_History]
        BK[Bookings]
        AV[Availability_Calendar]
        NT[Notifications]
    end

    subgraph analytics_db
        PV[Property_Views_Aggregated]
        PM[Platform_Metrics]
    end
```

**Design principle:** No foreign keys cross a service/database boundary. Where one service needs to reference an entity owned by another (e.g., a Booking referencing a `property_id`), it stores the ID as a plain reference plus a denormalized snapshot of the fields it needs (see SRS Section 2.2) — not a DB-level FK, since PostgreSQL can't enforce a foreign key across two separate databases.

### 4.2 Core Entity Relationships (within Listing Service)

```mermaid
erDiagram
    PROPERTY ||--o{ PROPERTY_IMAGE : has
    PROPERTY ||--o{ REVIEW : receives
    PROPERTY }o--|| CITY : located_in
    PROPERTY {
        uuid id PK
        string title
        string listing_type
        decimal price
        string status
        uuid builder_id FK
        decimal latitude
        decimal longitude
    }
    PROPERTY_IMAGE {
        uuid id PK
        uuid property_id FK
        string url
        string category
    }
    REVIEW {
        uuid id PK
        uuid property_id FK
        uuid buyer_id
        int rating
        string comment
        uuid booking_id
    }
    CITY {
        uuid id PK
        string name
        string state
    }
```

### 4.3 Core Entity Relationships (within Engagement Service)

```mermaid
erDiagram
    LEAD ||--o{ LEAD_STAGE_HISTORY : tracks
    BOOKING ||--o| LEAD : generates
    BOOKING }o--|| AVAILABILITY_SLOT : consumes
    LEAD {
        uuid id PK
        uuid buyer_id
        uuid property_id
        string stage
        uuid assigned_broker_id
    }
    LEAD_STAGE_HISTORY {
        uuid id PK
        uuid lead_id FK
        string from_stage
        string to_stage
        timestamp changed_at
    }
    BOOKING {
        uuid id PK
        uuid buyer_id
        uuid property_id
        uuid broker_id
        string status
        uuid rescheduled_from FK
    }
    AVAILABILITY_SLOT {
        uuid id PK
        uuid broker_id
        date slot_date
        time slot_time
        boolean is_booked
    }
```

---

## 5. Technology Stack

```mermaid
flowchart TB

subgraph Presentation Layer
    React["React + Vite"]
    Tailwind["Tailwind CSS"]
    Query["TanStack Query"]
    Charts["Recharts / Leaflet"]
end

subgraph Backend Layer
    Node["Node.js + Express"]
    Prisma["Prisma ORM"]
    Zod["Zod Validation"]
    Auth["JWT + Passport.js"]
end

subgraph Data Layer
    PostgreSQL["PostgreSQL"]
    Redis["Redis Cache / PubSub"]
    MinIO["MinIO Object Storage"]
end

subgraph Platform Layer
    Docker["Docker"]
    Kubernetes["Kubernetes"]
    Helm["Helm"]
    Argo["ArgoCD"]
    GitHub["GitHub Actions"]
end

subgraph Infrastructure
    Nginx["Nginx Gateway"]
    Prometheus["Prometheus"]
    Grafana["Grafana"]
    TLS["Let's Encrypt"]
end

React --> Node
Node --> PostgreSQL
Node --> Redis
Node --> MinIO

Docker --> Kubernetes
Helm --> Kubernetes
Argo --> Kubernetes
GitHub --> Docker

Nginx --> Node
Prometheus --> Node
Grafana --> Prometheus
TLS --> Nginx
```

| Layer | Technology | Cost |
|---|---|---|
| Frontend | React + Vite, JavaScript (ES6+), Tailwind, shadcn/ui, TanStack Query, Recharts, Leaflet.js | Free/OSS |
| Backend (per service) | Node.js, Express.js, JavaScript, Prisma, Zod | Free/OSS |
| Auth | JWT, Passport.js (Google OAuth2), bcrypt | Free/OSS |
| Database | PostgreSQL (one per service) | Free/OSS |
| Cache & Events | Redis (cache + Pub/Sub) | Free/OSS |
| Storage | MinIO (self-hosted, S3-compatible) | Free/OSS — fully self-hosted, no vendor account |
| Maps | OpenStreetMap, Overpass API, Nominatim, Leaflet.js | Free/OSS |
| Containers & Orchestration | Docker, Kubernetes, Helm | Free/OSS |
| GitOps / CI | ArgoCD, GitHub Actions | Free/OSS |
| Gateway | Nginx | Free/OSS |
| Observability | Prometheus, Grafana | Free/OSS |
| TLS | Let's Encrypt | Free |

---

## 6. Integrations

```mermaid
graph LR
    subgraph Platform
        IS[Identity Service]
        LS[Listing Service]
        ES[Engagement Service]
    end

    GOOGLE["Google OAuth2<br/>(login/signup)"]
    OSM["OpenStreetMap Ecosystem<br/>(Overpass API + Nominatim + Leaflet)"]
    SMTP["Self-hosted SMTP<br/>(Nodemailer)"]
    SMS["OSS/Free-tier SMS Gateway"]
    MINIO["MinIO<br/>(self-hosted, images + documents)"]
    NGINXCACHE["Nginx Caching Layer"]

    IS -->|OAuth2 consent + token exchange| GOOGLE
    LS -->|nearby schools/hospitals/metro, geocoding| OSM
    ES -->|booking/lead notifications| SMTP
    ES -->|booking/lead notifications| SMS
    LS -->|property images| MINIO
    IS -->|verification documents| MINIO
    MINIO -->|served via| NGINXCACHE
```

| Integration | Used By | Purpose | Notes |
|---|---|---|---|
| Google OAuth2 | Identity Service | Social login/signup | Free to integrate; new users default to `Buyer` role |
| OpenStreetMap + Overpass API + Nominatim | Listing Service | Nearby infrastructure (schools/hospitals/metro), geocoding | Free alternative to paid Google Maps API |
| Self-hosted SMTP (Nodemailer) | Engagement Service | Email notifications | Best-effort channel; in-app remains guaranteed |
| OSS/free-tier SMS gateway | Engagement Service | SMS notifications | Best-effort channel; avoids paid Twilio/MSG91 |
| MinIO + Nginx caching | Listing Service, Identity Service | Image and document storage/delivery | Fully self-hosted, S3-compatible — no vendor account or egress cost |

---

## 7. Key Flow — Booking Sequence (Cross-Service)

> **Note:** `architecture-beta` doesn't model time-ordered messages the way a true sequence diagram does — the numbers in each node label preserve the step order shown below. For strict message-level timing (request/response arrows per call), see the sequence diagram already used in the SRS.

```mermaid
flowchart TB
    Buyer([Buyer])
    Gateway["API Gateway"]
    subgraph Booking Process
        Engagement["Engagement Service"]
        Listing["Listing Service"]
        Booking["Booking Transaction"]
    end
    subgraph Event Processing
        Redis["Redis Pub/Sub"]
        Analytics["Analytics Service"]
    end
    subgraph Notification
        Email["Email"]
        SMS["SMS"]
        App["In-App Notification"]
    end
    Buyer --> Gateway
    Gateway --> Engagement
    Engagement --> Listing
    Listing --> Engagement
    Engagement --> Booking
    Booking --> Redis
    Redis --> Analytics
    Booking --> Email
    Booking --> SMS
    Booking --> App
    Email --> Buyer
    SMS --> Buyer
    App --> Buyer
```

**Step order:** (1) Buyer submits a booking request → (2) Gateway routes it to the Engagement Service → (3) Engagement Service receives it → (4) Engagement Service calls Listing Service internally to snapshot property price/builder info → (5) Booking creation + Lead stage update happen as a single local DB transaction inside Engagement Service → (6) Engagement Service publishes a `booking.created` event to Redis → (7) Analytics Service consumes the event asynchronously and updates its aggregation tables → (8) Buyer is notified in-app (guaranteed) plus email/SMS (best-effort).

---

## 8. Deployment View

```mermaid
flowchart LR

%% ---------------- CI/CD ----------------
subgraph CI_CD["CI/CD Pipeline"]
    Git["GitHub Repository"]
    Jenkins["Jenkins"]
    Sonar["SonarQube<br/>Code Quality"]
    Trivy["Trivy<br/>Container Scan"]
    Docker["Docker Build"]
    Registry["Container Registry"]
    Helm["Helm Charts"]
    Argo["ArgoCD"]
end

Git --> Jenkins
Jenkins --> Sonar
Sonar --> Trivy
Trivy --> Docker
Docker --> Registry
Registry --> Argo
Helm --> Argo

%% ---------------- Kubernetes ----------------
subgraph K8s["Kubernetes Cluster"]

    Ingress["Nginx Ingress"]

    Istio["Istio Service Mesh"]

    subgraph Services["Microservices"]
        Identity["Identity Service"]
        Listing["Listing Service"]
        Finance["Finance Service"]
        Preference["Preference Service"]
        Discovery["Discovery Service"]
        Engagement["Engagement Service"]
        Analytics["Analytics Service"]
    end

    subgraph Storage["Data Layer"]
        PostgreSQL["PostgreSQL<br/>StatefulSets (x7)"]
        Redis["Redis Cache / Pub-Sub"]
        MinIO["MinIO Object Storage"]
    end

    subgraph Monitoring["Observability"]
        Prometheus["Prometheus"]
        Grafana["Grafana"]
        Loki["Loki"]
        Alert["Alertmanager"]
    end

end

%% Deployment
Argo --> Ingress
Ingress --> Istio

Istio --> Identity
Istio --> Listing
Istio --> Finance
Istio --> Preference
Istio --> Discovery
Istio --> Engagement
Istio --> Analytics

%% Databases
Identity --> PostgreSQL
Listing --> PostgreSQL
Finance --> PostgreSQL
Preference --> PostgreSQL
Discovery --> PostgreSQL
Engagement --> PostgreSQL
Analytics --> PostgreSQL

Listing --> MinIO
Identity --> MinIO

Engagement --> Redis
Analytics --> Redis

%% Monitoring
Identity -. Metrics .-> Prometheus
Listing -. Metrics .-> Prometheus
Finance -. Metrics .-> Prometheus
Preference -. Metrics .-> Prometheus
Discovery -. Metrics .-> Prometheus
Engagement -. Metrics .-> Prometheus
Analytics -. Metrics .-> Prometheus

Prometheus --> Grafana
Prometheus --> Alert
Loki --> Grafana
```

**Each service ships as its own Helm chart** (or a shared umbrella chart with per-service `values.yaml`), with its own ArgoCD `Application` manifest — meaning a rollback of the Analytics Service, for example, never touches the Listing or Engagement Service pods.

---

## 9. Summary

This HLD is the visual companion to the SRS's Section 2 (System Overview): 7 services, database-per-service, Redis doing double duty as cache and event bus, an Nginx gateway at the edge, and a GitOps deployment model via ArgoCD/Helm/Kubernetes. The next level of detail — per-service Low-Level Design (class/module structure, exact table schemas, detailed sequence diagrams for every flow) — would be the natural follow-up document if needed.
